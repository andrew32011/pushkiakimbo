using UnityEngine;

namespace CrowdRunner
{
    // Выбор уровней: карта эпох, прогресс, выбор открытой эпохи для фарма.
    // Каркас; содержимое (эпохи/замки) наполняется в Стадии 3.
    public class LevelSelectUI : UIPanel
    {
        // OnClose/OnMenu — из базового UIPanel. Эпохи/замки наполняются в Стадии 3.
    }
}
