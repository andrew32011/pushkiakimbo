using UnityEngine;

namespace CrowdRunner
{
    // Кейсы: анимация открытия, содержимое (оружие + монеты + пассивки), покупка за монеты/кристаллы.
    // Каркас; содержимое наполняется в Стадии 3.
    public class CasesUI : UIPanel
    {
        // OnClose/OnMenu — из базового UIPanel. Содержимое наполняется в Стадии 3.
    }
}
